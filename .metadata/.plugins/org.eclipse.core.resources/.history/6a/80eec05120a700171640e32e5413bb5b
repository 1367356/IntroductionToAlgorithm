package class03;

import java.util.List;

import class02.BFS;
import class02.BFS.Node;

public class DFS {
	
	BFS bfs=new BFS();
	public void dfs(){
		List list=bfs.createGraph();
		for (int i = 0; i < list.size(); i++) {
			Node node=(Node) list.get(i);
		}
	}
}
